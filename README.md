# Steel interview project

This project is a mini proxy server written in Python. It uses `uv` for project setup. Assuming you
have that set up, you can run the project using the command `uv run fastapi dev`.
